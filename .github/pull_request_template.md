## What

Brief description of the change.

## Why

The problem it solves or the issue it closes.

## Checklist

- [ ] `make test` passes
- [ ] `cd ui && npm run lint && npm run build` passes (if UI changed)
- [ ] Schema changes are appended migrations (never edits to existing ones)
